local StarterGui = game:GetService("StarterGui")
local function sendSystemNotification(title, msg, duration)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = msg,
            Duration = duration or 5,
            Button1 = "OK"
        })
    end)
end
local games = {
    [93978595733734] = "https://raw.githubusercontent.com/ranz-coder/roblox-sc/refs/heads/script/LaPer-UNI", -- Violence District
}

local fallbackUniversalURL = "https://raw.githubusercontent.com/ranz-coder/roblox-sc/refs/heads/script/LaPer-UNI"

local currentPlaceId = game.PlaceId
local targetScriptURL = games[currentPlaceId]

if targetScriptURL then
    sendSystemNotification("LaperGank System", "Map Tertentu nih si anjing! Sabar loading HUB kontol...", 3)
else
    sendSystemNotification("LaperGank System", "Map Bebas nih si anjing! Sabar loading Kontol...", 3)
    targetScriptURL = fallbackUniversalURL
end

if targetScriptURL then
    local success, scriptContent = pcall(function()
        return game:HttpGet(targetScriptURL)
    end)
    
    if success and scriptContent then
        local executeScript, err = loadstring(scriptContent)
        if executeScript then
            local runSuccess, runError = pcall(executeScript)
            if runSuccess then
                sendSystemNotification("Laper Gank SAY", "LAPERGANK HUB NYALA NIH ANJINGG!", 3)
            else
                sendSystemNotification("Runtime Error", "Script utama gagal berjalan: " .. tostring(runError), 5)
            end
        else
            sendSystemNotification("Loader Error", "Gagal memproses kode: " .. tostring(err), 5)
        end
    else
        sendSystemNotification("Connection Error", "Gagal ngambil data si anjing, nunggu owner benerin ya kontol!", 5)
    end
end
